apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "webserver.fullname" . }}-environment
  labels:
    {{- include "webserver.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.configMap.env | nindent 2 }}
